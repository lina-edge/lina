# Managed by Ansible — see https://github.com/ncabatoff/process-exporter
# Docker (evaluation edge): keep in sync with infrastructure/process-exporter/process-exporter.yaml
process_names:
  - comm:
      - device
      - ledger
      - consumption
      - lightning
      - nanomq
      - redis-server
